{% extends 'base.html.twig' %}

{% block title %}Mascota{% endblock %}

{% block body %}
    <h1>Mascota</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mascotum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mascotum.name }}</td>
            </tr>
            <tr>
                <th>Edad</th>
                <td>{{ mascotum.edad }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ mascotum.created ? mascotum.created|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mascota_index') }}">back to list</a>

    <a href="{{ path('app_mascota_edit', {'id': mascotum.id}) }}">edit</a>

    {{ include('mascota/_delete_form.html.twig') }}
{% endblock %}
